import { makeStyles } from "@fluentui/react-components";

export const useHabitPageStyles = makeStyles({
    footer: {
        display: "flex",
        gap: "8px",
        alignItems: "center",
        justifyContent: "flex-end",
    },
});
